# 백엔드 연동 가이드

학습 관리 앱에 PDF AI 요약 백엔드를 연동하는 방법을 안내합니다.

## 📋 기능

- PDF 파일 업로드
- OpenAI GPT-4를 사용한 페이지별 AI 요약
- 공책 스타일 전체 요약 생성
- 요약 PDF 다운로드

## 🔧 백엔드 설정

### 1. 필요한 패키지 설치

백엔드 디렉토리에서 다음 명령어를 실행하세요:

```bash
pip install fastapi uvicorn python-dotenv pypdf reportlab openai python-multipart
```

### 2. OpenAI API 키 설정

백엔드 디렉토리에 `.env` 파일을 생성하고 OpenAI API 키를 추가하세요:

```
OPENAI_API_KEY=your_openai_api_key_here
```

### 3. 백엔드 서버 실행

백엔드 디렉토리에서 다음 명령어로 서버를 실행하세요:

```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

또는 Python으로 직접 실행:

```bash
python main.py
```

서버가 실행되면 `http://localhost:8000`에서 API에 접근할 수 있습니다.

## 🌐 프론트엔드 설정

### 1. 환경 변수 설정

프론트엔드 루트 디렉토리에 `.env` 파일을 생성하고 백엔드 API URL을 설정하세요:

```
VITE_API_URL=http://localhost:8000
```

배포 환경에서는 실제 백엔드 URL로 변경하세요:

```
VITE_API_URL=https://your-backend-api.com
```

### 2. 프론트엔드 실행

환경 변수를 설정한 후 프론트엔드를 실행하세요. 환경 변수 변경 시 서버를 재시작해야 합니다.

## 📚 사용 방법

1. **과목 추가**: 하단 메뉴에서 ➕ 버튼 클릭
2. **PDF 업로드**: PDF 파일 선택 (AI가 자동으로 분석 시작)
3. **과목명 입력**: 과목명과 설명(선택) 입력 후 저장
4. **노트 확인**: 과목 선택 → 노트정리 탭 → 노트 보기
5. **PDF 다운로드**: 노트정리 화면에서 'PDF 다운로드' 버튼 클릭

## 🔍 API 엔드포인트

### POST `/api/summarize-pdf`
- PDF 파일을 업로드하고 AI 요약을 생성합니다
- Request: multipart/form-data (file: PDF)
- Response: HierarchicalSummary (요약 데이터)

### GET `/api/summary-pdf/{document_id}`
- 생성된 요약 PDF를 다운로드합니다
- Request: document_id (path parameter)
- Response: PDF 파일

## ⚠️ 주의사항

- OpenAI API 사용에 따른 비용이 발생할 수 있습니다
- PDF 크기가 클수록 처리 시간이 오래 걸립니다
- 백엔드 서버가 실행 중이어야 PDF 업로드 기능을 사용할 수 있습니다
- 백엔드 서버가 실행되지 않은 상태에서는 기존 목업 데이터가 표시됩니다

## 🐛 문제 해결

### "PDF 요약 생성에 실패했습니다" 오류
- 백엔드 서버가 실행 중인지 확인하세요
- CORS 설정이 올바른지 확인하세요
- OpenAI API 키가 올바르게 설정되었는지 확인하세요

### 환경 변수가 적용되지 않음
- 개발 서버를 재시작하세요
- `.env` 파일이 프로젝트 루트에 있는지 확인하세요
- 변수명이 `VITE_`로 시작하는지 확인하세요 (Vite 프로젝트)

## 🚀 배포

배포 시 다음 사항을 확인하세요:

1. 백엔드 서버를 별도로 배포 (예: AWS, Heroku, Railway 등)
2. 프론트엔드의 `VITE_API_URL`을 배포된 백엔드 URL로 변경
3. 백엔드의 CORS 설정에 프론트엔드 도메인 추가
4. HTTPS 사용 권장
